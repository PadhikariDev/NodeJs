// //files handling is node js 

// //can create files and read them

// //need modules named fs to work on with file handling
// import fs from 'fs';

// // //creating file
// // const f= fs.writeFileSync("fileTest.txt","creating files and overding it ");

// //reading the existing file

// // const r= fs.readFileSync("./fileTest.txt","utf-8");

// // //deleting the files
// // const d =fs.unlinkSync("fileTest.txt");

// // can append to existing file
// // const ap = fs.appendFileSync("fileTest.txt",`\n${new Date()}`)

//some of the used ones are : Reading files: fs.readFile(), fs.readFileSync()

// Writing to files: fs.writeFile(), fs.writeFileSync()

// Appending to files: fs.appendFile(), fs.appendFileSync()

// Deleting files: fs.unlink(), fs.unlinkSync()

// Directory handling: fs.mkdir(), fs.rmdir(), fs.readdir()

// File renaming: fs.rename()

// Getting file stats: fs.stat()

// File access check: fs.access()

// export default {ap};

